spring:
  # DB
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:~/jerryshop
    driverClassName: org.h2.Driver
    username: jerryshop
    password: 
    
  # JPA
  jpa:
    properties:
      hibernate:
        dialect:
          org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
      
  # 메일 바꿨당
  mail :
    host : smtp.naver.com
    port : 465
    username : leejw2013@naver.com
    password : wndnjsWidmd51
    properties :
      mail : 
        smtp : 
          auth : true
          ssl : 
            enable : true